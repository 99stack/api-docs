description: Access Denied
content:
  application/json:
    schema:
      type: object
      description: 
      properties:
        response:
          type: string
          description: HTTP error code and text representation
        reason:
          type: string
          description: Details about why the request failed
          
    example:
      {
        "errors": {
          "response": "403 Access Denied",
          "reason": "Access denied, your account does not have permission to use this feature"
        }
      }
